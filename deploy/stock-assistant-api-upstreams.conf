# Managed by deploy/scripts/rollout-api-release.sh during rolling updates.
# Keep at least one server active; never expose these loopback ports publicly.
server 127.0.0.1:8001 max_fails=1 fail_timeout=10s;
server 127.0.0.1:8002 max_fails=1 fail_timeout=10s;
